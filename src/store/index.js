import { createStore } from 'vuex'
import portfolio from '@/store/modules/portfolio'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    portfolio
  }
})
